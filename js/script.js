function init(){
//add your javascrip between these two lines of code
  var textentry=entryinput;
  window.alert("Xing Chen, "+textentry);
  h2.document.write("a");
  window.addEventListener("click", init);
}
